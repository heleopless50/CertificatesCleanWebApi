namespace CleanWebApi.Domain.Constants;

public abstract class Roles
{
    public const string Administrator = nameof(Administrator);
    public const string TrainingCenterManager = nameof(TrainingCenterManager);

}
